{
  "_comment": "Written at deploy time by the app-deploy skill. Tells the app which database and schema hold its objects, so it never has to infer them from session context.",
  "database": "{{ db }}",
  "schema": "{{ schema }}",
  "agent_name": "{{ cfg.agent_name }}"
}
