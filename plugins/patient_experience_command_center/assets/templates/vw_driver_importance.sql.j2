-- VW_DRIVER_IMPORTANCE — model-derived global driver ranking (what moves detractor risk most).
CREATE OR REPLACE VIEW {{ target('VW_DRIVER_IMPORTANCE') }} AS
SELECT RANK, DRIVER, IMPORTANCE FROM {{ target('MDL_DRIVER_IMPORTANCE') }} ORDER BY RANK;
